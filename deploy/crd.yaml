apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: clusterdnses.dns.openshift.io
spec:
  group: dns.openshift.io
  names:
    kind: ClusterDNS
    listKind: ClusterDNSList
    plural: clusterdnses
    singular: clusterdns
  scope: Namespaced
  version: v1alpha1
